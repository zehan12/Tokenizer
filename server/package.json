{
  "name": "server",
  "version": "1.0.50",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build --outdir=build --minify ./src/index.ts",
    "start": "bun run ./build/index.js",
    "postbuild": "chmod +x ./build/index.js"
  },
  "dependencies": {
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "module": "src/index.js"
}